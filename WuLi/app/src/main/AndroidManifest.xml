<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acer.wuli">

    <!-- 储存卡的读写权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 调用相机权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 允许联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 获取GSM（2g）、WCDMA（联通3g）等网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 获取wifi网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 获取sd卡写的权限，用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 允许读取手机状态 用于创建BmobInstallation -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />


    <!--android:name="com.mob.MobApplication"-->

    <application

        android:name=".Application.SysApplication"

        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>



        <activity android:name=".main.activity.AddActivity">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->
                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>

        <activity android:name=".login.activity.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".login.activity.LoginActivity" />
        <activity android:name=".login.activity.ForgetActivity" />
        <activity android:name=".login.activity.WelcomeActivity" />
        <activity android:name=".login.activity.RegisterActivity" />

        <activity android:name=".lichenhao.activity.MainActivity" />

        <activity android:name=".collect_feedback.activity.ReturnActivity" />
        <activity android:name=".collect_feedback.activity.AboutActivity"></activity>
        <activity android:name=".history_hot.activity.HistoryActivity"></activity>

        <activity android:name=".addcomment.activity.Comment_Add_Activity">
        </activity>
        <activity android:name=".comments.activity.CommentsActivity" />

        <activity android:name=".collect_feedback.activity.HomeActivity" />

        <activity android:name=".main.activity.MovieListActivity" />
        <activity android:name=".comments.activity.OneCommentActivity" />

        <activity android:name=".lichenhao.activity.UserInfoActivity" />



        <!--图片选择Activity-->
        <activity
            android:name="com.donkingliang.imageselector.ImageSelectorActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />


        <!--图片预览Activity-->
        <activity
            android:name="com.donkingliang.imageselector.PreviewActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />


        <!--图片剪切Activity-->
        <activity
            android:name="com.donkingliang.imageselector.ClipImageActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <activity android:name=".collect_feedback.activity.MainStartActivity0" />

        <!-- key和secret -->
        <meta-data android:name="Mob-AppKey" android:value="d580ad56b4b5"/>
        <meta-data android:name="Mob-AppSecret" android:value="7fcae59a62342e7e2759e9e397c82bdd"/>

    </application>

</manifest>